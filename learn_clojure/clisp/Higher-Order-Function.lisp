; 고차 함수
; 리스프의 대표적인 고차 함수로는 mapcar, remove-if, reduce가 있다.

; mapcar는 함수와 리스트를 인자로 받아들여서 리스트의 각 요소에 함수를 적용한 결과값으로 구성된
; 리스트를 반환한다.
(mapcar
  (lambda (x) (+ x 1))
  '(1 2 3))
; (2 3 4)

; remove-if 함수는 함수와 리스트를 인자로 받아들이고 리스트의 각 요소에 함수를 적용해서 결과값이
; true인 요소를 제외한 리스트를 반환한다.
(remove-if
  (lambda (x) (= x 2))
  '(1 2 3))
; (1 3)

; reduce는 두 개의 요소씩 적용되어 최종적으로 하나의 값을 반환한다.
(reduce
  (lambda (x y) (+ x y))
  '(1 2 3 4 5))
; 15
