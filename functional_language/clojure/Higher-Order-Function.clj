; 리스트에 고차 함수 적용
; 리스트에 대한 고차 함수인 map, reduce, filter를 사용하여 람다 함수를 적용하는 것은 함수형
; 언어의 기본 테크닉이자 빅 데이터 처리 프레임워크에서도 사용되는 테이터 처리 테크닉이다.

(map (fn [x] (* x x)) '(1 2 3))
; (1 4 9)

(reduce (fn [x y] (+ x y)) '(1 2 3))
; 6

(filter (fn [x] (> x 2)) '(1 2 3 4 5))
; (3 4 5)
